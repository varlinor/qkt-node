---
description: Monorepo 工作区、构建与测试使用说明（CLI/构建产物位置）
---
# Monorepo 工作区与脚本/构建说明

- 包管理
  - 使用 PNPM 工作区；根目录含 `pnpm-workspace.yaml`
  - 各包有独立 `package.json` 与 `build.config.ts`

- 构建
  - 各包通过自身构建配置产出到 `dist/`
  - 常见命令（示例，具体以各包 `package.json` 为准）：
    - 在包内执行：`pnpm -C packages/cli build`
    - 全局构建（若有脚本）：`pnpm -r build`

- 运行/测试（示例）
  - CLI（查看可用子命令）：`pnpm -C packages/cli start` 或直接运行其构建产物
  - Node 工具与插件：优先从 `packages/node-tools/dist/` 产物调试，源码在 `src/`
  - 集成测试样例位于独立工作区 `test/`：
    - 入口测试：[test/src/testGenSfcDefs.js](mdc:test/src/testGenSfcDefs.js)
    - 示例目录结构：[test/testDir/](mdc:test/testDir)

- 定位问题建议
  - CLI 相关：从 [packages/cli/src/index.ts](mdc:packages/cli/src/index.ts) 入手
  - 构建/解析相关：
    - Vue3 Hooks 与动态导入：`packages/builder-vue3/src/`
    - Node 插件（Glob/目录/动态导入/SVG/SFC）：`packages/node-tools/src/plugins/`

- 其他
  - 统一 TS 配置在根 `tsconfig.base.json` 与 `tsconfig.json`
  - Babel 配置 `babel.config.js`，仅在需要时生效
